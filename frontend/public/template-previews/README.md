Static template preview images live here.

Expected files:

- `classic.webp`
- `modern.webp`
- `art.webp`

These images should be generated from the real Typst templates with the sample resume data, then committed as static assets. The templates page reads these files directly and does not hand-render resume previews in React/CSS.
